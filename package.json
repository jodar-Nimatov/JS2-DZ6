{
  "name": "sneackers_start_from_the_begin",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server-auth --watch db.json --port 8080"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.1",
    "json-server-auth": "^2.1.0"
  }
}
